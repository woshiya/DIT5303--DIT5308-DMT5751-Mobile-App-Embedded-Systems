<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <!-- Permissions -->
    <uses-permission android:name="android.permission.BLUETOOTH" />
    <uses-permission android:name="android.permission.BLUETOOTH_ADMIN" />
    <uses-permission android:name="android.permission.BLUETOOTH_CONNECT" />
    <uses-permission android:name="android.permission.BLUETOOTH_SCAN" />
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.SCHEDULE_EXACT_ALARM" />
    <uses-permission android:name="android.permission.USE_EXACT_ALARM" />
    <uses-permission android:name="android.permission.VIBRATE" />
    <uses-permission android:name="android.permission.WAKE_LOCK" />

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.IOTMedicineBox"
        tools:targetApi="31">

        <!-- Home Activity -->
        <activity
            android:name=".HomeActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- MedBox Activity -->
        <activity
            android:name=".MedBoxActivity"
            android:exported="false" />

        <!-- Chat Activity -->
        <activity
            android:name=".ChatActivity"
            android:exported="false" />

        <!-- Reminder Activity -->
        <activity
            android:name=".ReminderActivity"
            android:theme="@style/Theme.AppCompat.Light.NoActionBar"
            android:launchMode="singleInstance"
            android:excludeFromRecents="true"
            android:showOnLockScreen="true"
            android:turnScreenOn="true"
            android:showWhenLocked="true"
            android:exported="false"
            tools:ignore="AppLinkUrlError">

            <!-- intent-filter 必须放在 activity 标签内 -->
            <intent-filter>
                <action android:name="android.intent.action.VIEW" />
                <category android:name="android.intent.category.DEFAULT" />
            </intent-filter>
        </activity>

        <!-- Bluetooth Service -->
        <service
            android:name=".BluetoothService"
            android:exported="true" />

        <!-- Kommunicate Meta Data -->
        <meta-data
            android:name="com.kommunicate.applicationId"
            android:value="373747033702dd772bcb38799fa7e9a77" />

        <!-- AddMedicine Activity -->
        <activity
            android:name=".AddMedicineActivity"
            android:label="Add Medicine"
            android:theme="@style/Theme.AppCompat.Light.NoActionBar"
            android:exported="true" />

        <!-- Tracker Activity -->
        <activity
            android:name=".TrackerActivity"
            android:exported="false" />
        <!-- Profile Activity -->
        <activity
            android:name=".ProfileActivity"
            android:exported="false" />
    </application>
</manifest>